{{template "base" .}}
{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>This is the about page</h1>
            <p>This is a paragraph of text</p>

            <p>This came from template: {{index .StringMap "test"}}</p>
            <p>
                {{if ne (index .StringMap "remote_ip") ""}}
                    Your remte address is {{index .StringMap "remote_ip"}}
                {{else}}
                    I dont know your ip address, visite <a href="/">Home Page</a> so I can set it.
                {{end}}
            </p>
        </div>
    </div>
</div>
{{end}}